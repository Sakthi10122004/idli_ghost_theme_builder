import Builder from "@/components/builder/Builder";

export const metadata = {
  title: "Visual Builder | Ghost Theme Builder",
  description: "Visual drag-and-drop editor for modern Ghost CMS publication themes.",
};

export default function BuilderPage() {
  return <Builder />;
}
